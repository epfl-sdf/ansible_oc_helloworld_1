apiVersion: v1
kind: Route
metadata:
    name: my-nginx-app
spec:
    host: my-nginx-app-myproject.192.168.99.100.nip.io
    port:
        targetPort: 80
    to:
        kind: Service
        name: my-nginx-app
